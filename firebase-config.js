// Paste your Firebase project's config here (Firebase Console → Project settings →
// scroll to "Your apps" → Web app → SDK setup and configuration → "Config").
// If you leave apiKey empty, the app runs fine in local-only mode (no cloud backup).
window.FIREBASE_CONFIG = {
  apiKey: "",
  authDomain: "",
  projectId: "",
  storageBucket: "",
  messagingSenderId: "",
  appId: ""
};
